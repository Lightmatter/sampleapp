{% if use_tag %}
    <{{ tag|default:'label' }}
    {% include 'django/forms/attrs.jinja' %}
    class="block text-sm font-medium text-gray-700">{{ field.label }}</{{ tag|default:"label" }}>
{% else %}
    {{ field.label }}
{% endif %}
